name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install ALSA headers (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev pkg-config

      - name: Install Rust
        run: rustup update stable && rustup default stable && rustup component add clippy rustfmt

      - name: Format
        # Formatting is OS-independent; checking it once off Windows also avoids
        # the CRLF-vs-LF `fmt --check` false positive on Windows runners.
        if: runner.os != 'Windows'
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Test
        run: cargo test

      - name: Build (release)
        run: cargo build --release
  supply-chain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: rustup update stable && rustup default stable

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked

      - name: Run cargo-deny supply-chain checks
        # Run advisories/bans/sources here; license checks are intentionally skipped
        # until the plugin VST3 licensing exception is fully scoped in CI policy.
        run: cargo deny check advisories bans sources
